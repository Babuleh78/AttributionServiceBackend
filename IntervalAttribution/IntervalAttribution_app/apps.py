from django.apps import AppConfig

class IntervalAttributionAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'IntervalAttribution_app'  # ← Должно быть именно так!